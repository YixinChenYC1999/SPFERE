# Server listening port should be same as client
LISTEN_HOST = "0.0.0.0"
LISTEN_PORT = 65432
# set client trainning timeout
CLIENT_TIMEOUT = 0
# Either to use cuda or not, for edge devices always set to False
USE_CUDA = False